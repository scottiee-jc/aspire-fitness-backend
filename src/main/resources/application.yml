spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aspirefitness
    username: arne
    password: slot
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: update
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
    show-sql: true

server:
  port: 8080
